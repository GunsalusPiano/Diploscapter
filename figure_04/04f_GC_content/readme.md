# GC content along the lengths of *Diploscapter* chromosomes

The file ```04f_GC_content.bash``` describes the commands to run the Python script ```fraction_GC_along_length.py```.

Plotting of the resulting data was done with the R ggplot2 code in ```genomic_features_visualizations.md``` in the main **Fig. 4** folder.
